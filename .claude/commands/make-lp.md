---
name: make-lp
description: Create an `index.html` landing page based on pico.css. 1) Support light/dark themes. 2) Support OGP (including X.com). 3) Support JSON-LD schema. 4) Set the favicon using an twemoji svg.
---

# Commands

Create an `index.html` landing page based on pico.css. 1) Support light/dark themes. 2) Support OGP (including X.com). 3) Support JSON-LD schema. 4) Set the favicon using an twemoji svg.